# HTTP API 호출
requests>=2.32.4

# 환경변수 관리
python-dotenv>=1.0.1

# DB ORM
sqlalchemy>=2.0.30

# DB 드라이버 (사용하는 DB에 따라 하나만 선택)
# pymysql>=1.1.0  # MySQL용 (PostgreSQL 사용 시 주석 처리)
psycopg2-binary>=2.9.10  # PostgreSQL용

# 설정 파일 파싱 (configparser는 Python 내장 모듈이므로 별도 설치 불필요) 